version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./frontend:/app/frontend
      - ./feedback.db:/app/feedback.db
    environment:
      - DATABASE_URL=sqlite:///./feedback.db
